version: "3"

services:
  nginx:
    image: nginx:latest
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nginx.rule=Host(`my-site.localhost`) && Path(`/api`)"
      - "traefik.http.routers.nginx.entrypoints=http"
    networks:
      - traefik_net

networks:
  traefik_net:
